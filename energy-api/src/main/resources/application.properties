spring.application.name=energy-api

server.port=8080

spring.datasource.url=jdbc:postgresql://localhost:5431/energy_db
spring.datasource.username=disysuser
spring.datasource.password=disyspw
spring.datasource.driver-class-name=org.postgresql.Driver

# The REST API only reads the tables, it never changes the schema
spring.jpa.hibernate.ddl-auto=none
