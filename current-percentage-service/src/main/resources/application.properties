spring.application.name=current-percentage-service

server.port=8082

spring.datasource.url=jdbc:postgresql://localhost:5431/energy_db
spring.datasource.username=disysuser
spring.datasource.password=disyspw
spring.datasource.driver-class-name=org.postgresql.Driver

# Tables are created by the usage-service (Flyway), so do not change the schema
spring.jpa.hibernate.ddl-auto=none
